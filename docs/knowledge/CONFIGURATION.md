# Project Configuration

The application reads configuration from:

    .env

Location:

Repository root.

Do not manually parse .env.

Use:

src/common/config.ts

Do not introduce dotenv or other configuration libraries unless explicitly requested.
